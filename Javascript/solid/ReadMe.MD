S - Single Responsibility:    
A class or module should have only one reason to change. In other words, it should have only one responsibility. | One class, one reason to change

O - Open/Closed:               
Software entities should be open for extension but closed for modification. | Extend functionality without modifying

L - Liskov Substitution:       
Subtypes must be replaceable for their base types without breaking the program. |Subtypes replace base types safely

I - Interface Segregation:     
Clients should not be forced to depend on methods they don’t use. Focused interfaces, avoid unused methods

D - Dependency Inversion:
High-level modules should not depend on low-level modules. Both should depend on abstractions. Depend on abstractions, not concretions
